using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaiTapBuoi1 : MonoBehaviour
{
    int b;
    int c;
    // Start is called before the first frame update
    void Start()
    {
        b = 1;
        c = 2;
        Debug.Log(b + c);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
}
